import { SimpleSearchpipePipe } from './simple-search.pipe';

describe('SimpleSearchpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleSearchpipePipe();
    expect(pipe).toBeTruthy();
  });
});
